import type { Metadata } from "next";
import "./globals.css";
import { Navbar } from "@/components/Navbar";
import { Footer } from "@/components/Footer";

export const metadata: Metadata = {
  title: "Melos | Structured Nutrition for Lasting Health and Results",
  description: "Melos Nutritional Consultancy & Therapy provides personalized nutrition plans based on full health assessment, body measurements, and lifestyle analysis in Addis Ababa, Ethiopia.",
  icons: {
    icon: "/melos_logo_white.png",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className="min-h-screen font-sans flex flex-col">
        <Navbar />
        <main className="flex-1">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
